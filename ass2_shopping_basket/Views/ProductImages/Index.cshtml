@model IEnumerable<ass2_shopping_basket.Models.ProductImage>
@*lab6b*@
@{

    @*ViewBag.Title = "Index";*@
//ViewBag.Title = "Upload New Image";
//lab6c
    ViewBag.Title = "Images";
}
<br />
<br />
<br />
<h2>@ViewBag.Title</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@

    @Html.ActionLink("Upload New Image", "Upload")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FileName)
        </th>
        <th></th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.FileName)
    </td>
    @*lab6c-show Thumbnail*@
    <td>
        @if (!String.IsNullOrWhiteSpace(item.FileName))
        {
            <img src="@(Url.Content(Constants.ProductThumbnailPath) + item.FileName)">
        }
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
        @Html.ActionLink("Details", "Details", new { id = item.ID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ID })
    </td>
</tr>
    }

</table>
